import type { ThemeConfig } from '@/stores/theme/typings'

export const themeConfig: ThemeConfig = {
  sidebarStyle: 'dark',
  headerStyle: 'light',
  primaryColor: '#409eff',
  layout: 'vertical',
  uniqueOpened: true,
  showLogo: true,
  showBreadcrumb: true,
  showTabsView: true,
  isTabsCache: true,
}
